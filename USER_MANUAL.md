# คู่มือการใช้งานระบบ Sudprodshop Checkout (User Manual)

ยินดีต้อนรับสู่ระบบ **Sudprodshop Checkout** ระบบตรวจเช็คพัสดุและสินค้าขาออกที่ออกแบบมาเพื่อความรวดเร็ว แม่นยำ และใช้งานง่าย รองรับการทำงานทั้งบนคอมพิวเตอร์และมือถือ

---

## 📚 สารบัญ
1. [สำหรับเจ้าหน้าที่คีย์ข้อมูล (Admin/Office)](#1-สำหรับเจ้าหน้าที่คีย์ข้อมูล-adminoffice)
   - [การเตรียมไฟล์ Excel/CSV](#11-การเตรียมไฟล์-excelcsv)
   - [การอัปโหลดข้อมูลเข้าระบบ](#12-การอัปโหลดข้อมูลเข้าระบบ)
   - [การดู Dashboard และสถานะ](#13-การดู-dashboard-และสถานะ)
   - [การส่งออกรายงาน (Export)](#14-การส่งออกรายงาน-export)
2. [สำหรับเจ้าหน้าที่สแกน (Scanner/Warehouse)](#2-สำหรับเจ้าหน้าที่สแกน-scannerwarehouse)
   - [การใช้งานเครื่องสแกนบนมือถือ](#21-การใช้งานเครื่องสแกนบนมือถือ)
   - [ความหมายของเสียงและสถานะ](#22-ความหมายของเสียงและสถานะ)
3. [การแก้ไขปัญหาเบื้องต้น](#3-การแก้ไขปัญหาเบื้องต้น)

---

## 1. สำหรับเจ้าหน้าที่คีย์ข้อมูล (Admin/Office)

### 1.1 การเตรียมไฟล์ Excel/CSV
ก่อนเริ่มงานในแต่ละวัน ต้องเตรียมไฟล์รายการพัสดุ (AWB) ที่จะจัดส่ง
- **รูปแบบไฟล์**: รองรับ `.xlsx` หรือ `.csv`
- **คอลัมน์ที่จำเป็น**: ต้องมีหัวตารางชื่อ `AWB`, `Tracking`, หรือ `Order No` (ระบบจะค้นหาอัตโนมัติ)
- **ตัวอย่างข้อมูล**:
  | AWB | Customer Name |
  |-----|---------------|
  | TH012345678 | Somchai |
  | TH987654321 | Somsri |

### 1.2 การอัปโหลดข้อมูลเข้าระบบ
1. เปิดหน้าเว็บ **Dashboard** บนคอมพิวเตอร์
2. ไปที่ส่วน **"Upload AWB File"**
3. คลิกปุ่ม **"Choose File"** แล้วเลือกไฟล์ที่เตรียมไว้
4. กดปุ่ม **"Upload"**
5. รอสักครู่ ระบบจะแจ้งจำนวนรายการที่นำเข้าสำเร็จ (เช่น "Imported 150 items")

### 1.3 การดู Dashboard และสถานะ
หน้าจอ Dashboard จะแสดงข้อมูลแบบ Real-time:
- **Total (ทั้งหมด)**: จำนวนพัสดุทั้งหมดในไฟล์ที่อัปโหลด
- **Scanned (สแกนแล้ว)**: จำนวนที่สแกนเจอและถูกต้อง (สีเขียว)
- **Missing (ขาด)**: จำนวนที่ยังไม่ได้สแกน (สีเหลือง)
- **Surplus (เกิน)**: พัสดุที่สแกนเจอแต่ **ไม่มี** ในไฟล์รายการ (สีแดง - ต้องตรวจสอบ!)

### 1.4 การส่งออกรายงาน (Export)
เมื่อจบงาน หรือต้องการตรวจสอบความคืบหน้า:
1. ไปที่ส่วน **"Export Data"**
2. เลือกประเภทรายงานที่ต้องการ:
   - **Export All**: ข้อมูลทั้งหมด
   - **Export Missing**: รายการที่ยังไม่ถูกสแกน (สินค้าตกหล่น)
   - **Export Surplus**: รายการที่เกินมา (สินค้าผิด/หลงมา)
   - **Export Scanned**: รายการที่ครบถ้วน
3. ไฟล์ Excel จะถูกดาวน์โหลดลงเครื่องทันที

---

## 2. สำหรับเจ้าหน้าที่สแกน (Scanner/Warehouse)

### 2.1 การใช้งานเครื่องสแกนบนมือถือ
1. เปิดหน้าเว็บผ่านมือถือ หรือแท็บเล็ต
2. กดปุ่ม **"Scanner Mode"** หรือไอคอนบาร์โค้ด
3. ระบบจะขออนุญาตใช้กล้อง -> กด **"Allow" (อนุญาต)**
4. นำกล้องไปส่องที่ Barcode หรือ QR Code บนกล่องพัสดุ
5. **เทคนิค**: ถือมือถือให้นิ่ง และให้บาร์โค้ดอยู่ในกรอบสี่เหลี่ยม

### 2.2 ความหมายของเสียงและสถานะ
ระบบจะมีเสียงแจ้งเตือนเพื่อให้ทำงานได้โดยไม่ต้องมองหน้าจอตลอดเวลา:

| สถานะ | สีบนหน้าจอ | เสียง | ความหมาย | สิ่งที่ต้องทำ |
|-------|------------|-------|----------|---------------|
| **✅ Match** | เขียว | "ติ๊ง!" (ใสๆ) | ถูกต้อง ข้อมูลตรงกัน | ผ่านไปกล่องถัดไปได้เลย |
| **⚠️ Duplicate** | เหลือง/ส้ม | "ตื๊ด-ตื๊ด" | ซ้ำ! สแกนไปแล้ว | แยกออก ไม่ต้องส่งซ้ำ |
| **❌ Surplus** | แดง | "ปี๊บ! ยาวๆ" | เกิน! ไม่มีข้อมูลในระบบ | **หยุด!** แยกกล่องนี้ออกทันที ตรวจสอบว่าผิดรอบหรือไม่ |

---

## 3. การแก้ไขปัญหาเบื้องต้น

- **สแกนไม่ติด**:
  - เช็คแสงสว่างให้เพียงพอ
  - บาร์โค้ดฉีกขาดหรือไม่? (ถ้าใช่ ให้กดปุ่ม "Keyboard" แล้วพิมพ์เลขเอา)
  - เลนส์กล้องมัวหรือไม่?
- **เสียงไม่ออก**:
  - ตรวจสอบว่าปิดเสียงมือถืออยู่หรือไม่
  - ลองกดปุ่มเพิ่มเสียง (Volume Up)
- **ข้อมูลไม่อัปเดต**:
  - ลองกด Refresh หน้าจอ (F5 หรือลากลง)
  - ตรวจสอบสัญญาณอินเทอร์เน็ต
